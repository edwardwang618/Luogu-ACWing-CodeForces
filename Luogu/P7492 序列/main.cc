#include <iostream>
using namespace std;

const int N = 1e5 + 10;
int n, m;
int a[N];
struct Node {
  int l, r;
  
}

int main() {
  scanf("%d%d", &n, &m);
  for (int i = 1; i <= n; i++) scanf("%ld", &a[i]);

}